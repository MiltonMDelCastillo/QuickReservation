@model IEnumerable<QuickReservation.Models.User>

<h1>List of Users</h1>


    <a href="@Url.Action("CreateUser", "User")" class="btn btn-primary">Create New User</a>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Role</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Id</td>
                <td>@user.Name</td>
                <td>@user.Email</td>
                <td>@user.RoleId</td>
                <td>
                    <a href="@Url.Action("EditUser", "User", new { id = user.Id })" class="btn btn-warning">Edit</a>
                    <a href="@Url.Action("DeleteUser", "User", new { id = user.Id })" class="btn btn-danger">Eliminate</a>
                </td>
            </tr>
        }
    </tbody>
</table>
